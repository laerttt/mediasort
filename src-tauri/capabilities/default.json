{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Default capability for mediasort",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
        "fs:scope-home-recursive",

    "fs:default",
    "shell:default"
  ]
}